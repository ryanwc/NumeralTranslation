package app;

/**
 * Handle queries regarding unit conversions and prices of various commodities
 * in the intergalactic market.
 *  
 * @author ryanwilliamconnor
 *
 */
public class QueryHandler {

	
	/*
	 * 		return note.qPos == note.components.length-1 && 
			note.countHow == 1 && note.howPos == 0 &&
			(note.countMuch == 1 || note.countMany == 1) && 
			(note.muchPos == 1 || note.manyPos == 1) &&
			note.countIs == 1 && note.isPos > 1 &&
			(note.countArabic + note.countComm + note.countCredits 
			 + note.countIntergalClust + note.countRoman >= 1) &&
			(note.isPos < note.arabicPos || note.isPos < note.creditPos ||
			 note.isPos < note.romanPos || 
			 note.isPos < note.start1IntergalClust || 
			 note.isPos < note.start2IntergalClust ||
			 note.isPos < note.comm1Pos || note.isPos < note.comm2Pos);
	 */
}
